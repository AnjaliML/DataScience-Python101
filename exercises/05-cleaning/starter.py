"""Starter: measure quality failures without silently repairing them."""

import pandas as pd


def quality_report(customers: pd.DataFrame) -> dict[str, int]:
    """Count rows and selected schema failures."""

    # TODO: require the columns and return the five documented counts.
    raise NotImplementedError

"""Starter: standardise a feature matrix without losing its shape."""

import numpy as np


def standardize_columns(matrix: np.ndarray) -> np.ndarray:
    """Return column-standardised values with the input shape preserved."""

    # TODO: validate dimensions and variance, then standardise along axis 0.
    raise NotImplementedError
